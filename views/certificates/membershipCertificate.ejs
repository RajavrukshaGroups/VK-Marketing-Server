<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Membership Certificate</title>
    <style>
      body {
        font-family: "Georgia", serif;
        background: #f5f5f5;
      }

      .certificate {
        width: 900px;
        height: 650px;
        margin: 40px auto;
        background: #fff;
        border: 10px solid #9b1c1c;
        padding: 30px;
        position: relative;
      }

      .inner-border {
        border: 4px solid #c9a44c;
        height: 100%;
        padding: 20px;
      }

      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .logo {
        width: 90px;
      }

      .since {
        font-size: 18px;
        font-weight: bold;
        color: #9b1c1c;
      }

      .title {
        text-align: center;
        margin-top: 20px;
      }

      .title h1 {
        color: #1f3c88;
        margin-bottom: 5px;
      }

      .title span {
        background: #1f3c88;
        color: #fff;
        padding: 6px 14px;
        font-size: 14px;
        border-radius: 20px;
      }

      .content {
        margin-top: 40px;
        font-size: 18px;
        text-align: center;
        line-height: 1.8;
      }

      .company {
        font-size: 26px;
        font-weight: bold;
        margin: 20px 0;
      }

      .footer {
        display: flex;
        justify-content: space-between;
        margin-top: 60px;
        align-items: center;
      }

      .sign {
        text-align: center;
      }

      .sign img {
        width: 120px;
      }

      .seal {
        width: 90px;
      }

      .date {
        position: absolute;
        bottom: 30px;
        right: 50px;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <div class="certificate">
      <div class="inner-border">
        <div class="header">
          <img src="<%= fkcciLogo %>" class="logo" />
          <div class="since">Since 1916</div>
        </div>

        <div class="title">
          <h1>FEDERATION OF KARNATAKA</h1>
          <h1>CHAMBERS OF COMMERCE & INDUSTRY (FKCCI)</h1>
          <span>ORDINARY MEMBER</span>
        </div>

        <div class="content">
          This is to certify that
          <div class="company"><%= companyName %></div>
          has been enrolled as an <strong>Ordinary Member</strong> and is
          entitled to all the privileges of Membership as per the bylaws of
          FKCCI.
        </div>

        <div class="footer">
          <div class="sign">
            <img src="<%= sirMV %>" />
            <p><strong>Sir M. Visvesvaraya</strong></p>
          </div>

          <img src="<%= memberSeal %>" class="seal" />

          <div class="sign">
            <img src="<%= memberSign %>" />
            <p><strong>Secretary General</strong></p>
          </div>
        </div>

        <div class="date">Issued on: <%= issuedDate %></div>
      </div>
    </div>
  </body>
</html>
