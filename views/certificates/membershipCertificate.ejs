<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Membership Certificate</title>

    <style>
      body {
        margin: 0;
        background: #f2f2f2;
        font-family: "Times New Roman", serif;
      }

      /* ================= OUTER CERTIFICATE ================= */
      .certificate {
        width: 1000px;
        min-height: 700px; /* 🔥 changed from height to min-height */
        margin: 30px auto;
        background: #ffffff;
        border: 18px solid #8b1a1a; /* thicker red border */
        padding: 20px;
        box-sizing: border-box;
      }

      /* ================= INNER BORDER ================= */
      .inner-border {
        border: 6px solid #c9a44c; /* thicker gold border */
        padding: 60px 70px;
        box-sizing: border-box;

        display: flex;
        flex-direction: column;
      }

      /* ================= HEADER ================= */
      .header {
        text-align: center;
      }

      .fkcci-logo {
        width: 170px;
        margin-bottom: 12px;
      }

      .kannada {
        color: #b11212;
        font-weight: bold;
        font-size: 24px;
        margin-bottom: 6px;
      }

      .english {
        font-size: 26px;
        font-weight: bold;
        color: #1e3a8a;
        letter-spacing: 1px;
      }

      /* ================= CONTENT ================= */
      .content {
        text-align: center;
        font-size: 20px;
        line-height: 2.3;
        margin-top: 60px;
      }

      .company {
        font-size: 34px;
        font-weight: bold;
        font-style: italic;
        margin: 25px 0;
        text-transform: uppercase;
      }

      /* ================= BOTTOM SECTION ================= */
      .bottom-section {
        margin-top: auto; /* pushes signatures to bottom */
      }

      .signatures {
        margin-top: 60px;
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
      }

      .sign {
        text-align: center;
        width: 250px;
      }

      .sign img {
        width: 120px;
        margin-bottom: 10px;
      }

      .sign strong {
        font-size: 18px;
      }

      /* ================= DATE ================= */
      .date {
        text-align: right;
        font-size: 16px;
        margin-top: 30px;
      }
    </style>
  </head>

  <body>
    <div class="certificate">
      <div class="inner-border">
        <!-- HEADER -->
        <div class="header">
          <img src="<%= aitiflogo %>" class="fkcci-logo" />

          <div class="kannada">ಅಖಿಲ ಭಾರತ ವ್ಯಾಪಾರ ಮತ್ತು ಕೈಗಾರಿಕಾ ವೇದಿಕೆ</div>

          <div class="english">ALL INDIA TRADE AND INDUSTRIES FORUM</div>
        </div>

        <!-- CONTENT -->
        <div class="content">
          This is to certify that

          <div class="company"><%= companyName %></div>

          has been enrolled to
          <strong><%= membershipType %></strong>
          and are entitled to all the privileges of membership as set forth in
          the Bylaws of the All India Trade and Industries Forum.
        </div>

        <!-- SIGNATURES + DATE -->
        <div class="bottom-section">
          <div class="signatures">
            <div class="sign">
              <img src="<%= presidentSign %>" />
              <div><strong>C. VENKATESH</strong></div>
              <div>President</div>
            </div>

            <div class="sign">
              <img src="<%= secretarySign %>" />
              <div><strong>SATHYA PRAVEEN</strong></div>
              <div>General Secretary</div>
            </div>
          </div>

          <div class="date">Issued on: <%= issuedDate %></div>
        </div>
      </div>
    </div>
  </body>
</html>
